# Optional: Set these ONLY if frontend is hosted separately from backend (e.g. Vercel / Netlify)
# If deploying full-stack on Render / Railway / Heroku / Docker, leave these empty.

# VITE_API_URL=https://your-backend-service.onrender.com
# VITE_SOCKET_URL=https://your-backend-service.onrender.com
